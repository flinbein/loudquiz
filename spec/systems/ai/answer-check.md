# Проверка ответов

AI оценивает ответы игроков в обычных раундах.
Используется на фазе [round-review](../../game/phases.md#round-review)
в AI-режиме.

## Входные данные

```typescript
interface AnswerCheckInput {
  question: string;
  answers: Array<{
    playerName: string;
    answer: string;
  }>;
}
```

| Поле | Описание |
|---|---|
| `question` | Текст вопроса |
| `answers` | Список ответов (имя игрока + текст ответа) |

## Правила для AI

### System prompt

Роль: судья викторины, оценивающий ответы.

Инструкции:

**Правильность:**
- Ответ должен быть **верным** и **поддаваться проверке**.
- Если ответ нельзя проверить — **не засчитывать**.

**Расплывчатость:**
- Расплывчатые ответы, которые нельзя верифицировать — **не засчитывать**.
- Пример: на вопрос «Фрукт из Африки» ответ «Африканский фрукт» — не засчитывается.

**Уникальность:**
- Семантически одинаковые ответы **объединяются в группу** (один уникальный ответ).
- Пример: «Гагарин» и «Юрий Гагарин» — одна группа.
- **Исключение:** если вопрос допускает синонимы (например, «Синоним слова...»),
  оба ответа принимаются.

**Опечатки:**
- Небольшие опечатки **допускаются**, если вопрос не требует точного написания.

**Итог:**
- Написать короткий комментарий-итог раунда.

### User prompt

Содержит:
- Текст вопроса.
- Список ответов игроков.

## Результат

```typescript
interface AnswerCheckResult {
  results: Array<{
    playerName: string;
    accepted: boolean;
    group: number | null;
    note: string | null;
  }>;
  comment: string;
}
```

| Поле | Описание |
|---|---|
| `playerName` | Имя игрока |
| `accepted` | Ответ верный или нет |
| `group` | Номер группы, если объединён с другим ответом |
| `note` | Комментарий: почему засчитан, не засчитан, или объединён |
| `comment` | Общий итог раунда |

## Оспаривание

Если игроки не согласны с оценкой AI, ведущий нажимает «Оспорить» —
переход к ручной оценке. Подробнее:
[Host: Round — round-review](../../ui/host-screens/round.md#round-review).

## См. также

- [AI-интеграция](README.md)
- [Правила игры](../../game/rules.md) — критерии оценки
- [Подсчёт очков](../../game/scoring.md)
